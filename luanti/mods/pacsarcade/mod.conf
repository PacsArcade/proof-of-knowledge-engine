# mod.conf — Pac's Arcade Luanti mod 💜
# The voxel-verse front-end into the Federated Knowledge Engine.
# See docs/CONVENTIONS.md — this mod is a HOT-tier front-end; world state is
# authoritative in DB-2 via the state-sync service, never in the mod itself.

name = pacsarcade
description = Pac's Arcade — the voxel front-end of the Federated Knowledge Engine. Adds logic-gate levers that sync world state through state-sync (DB-2) and forwards in-game chat to Matrix via matrix-bridge.
# http_mod so we may use minetest.request_http_api() to reach node-local services.
# TODO: the server operator must add `pacsarcade` to secure.http_mods in minetest.conf.
depends =
optional_depends = default
